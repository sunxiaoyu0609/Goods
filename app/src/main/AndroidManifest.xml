<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fragment2">
    <application
        android:allowBackup="true"
        android:icon="@drawable/login"
        android:label="小商店"
        android:roundIcon="@drawable/login"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" android:icon="@drawable/login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisteredActivity" />
        <activity android:name=".goods_detail" />
        <activity android:name=".evaluate" />
        <activity android:name=".caculate" />
        <activity android:name=".AlarmActivity"/>
        <activity android:name=".game"/>
        <activity android:name=".downloadimg"/>
        <activity android:name=".rili"/>
        <activity android:name=".hualang"/>

        <receiver android:name=".AlarmReceiver" android:process=":remote"/>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>